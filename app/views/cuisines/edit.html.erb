<%= flash[:error_message] %>
<h1>Edite o nome da cozinha</h1>

<%= form_for @cuisine, url: cuisine_path do |f| %>

 <%= f.label :name, 'Nome' %>
 <%= f.text_field :name %>

 <%= f.submit 'Enviar' %>

<% end %>
