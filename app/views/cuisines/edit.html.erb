<% if @cuisine.errors.any? %>
  <%= @cuisine.errors.full_messages.each do |error| %>
    <div class="alert alert-danger" role="alert"> <%= error %> </div>
  <% end %>
<% end %>

<h1>Edite o nome da cozinha</h1>

<%= render 'form' %>
